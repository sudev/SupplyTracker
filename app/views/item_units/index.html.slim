h1 Listing item_units

table
  thead
    tr
      th
      th
      th

  tbody
    - @item_units.each do |item_unit|
      tr
        td = link_to 'Show', item_unit
        td = link_to 'Edit', edit_item_unit_path(item_unit)
        td = link_to 'Destroy', item_unit, data: { confirm: 'Are you sure?' }, method: :delete

br

= link_to 'New Item unit', new_item_unit_path
